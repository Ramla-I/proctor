"""Command-line interface for the proctor orchestration framework.

Planned verbs: run, resume, bench, report, stages, validate, warmup.
See plan_docs/orchestration_framework_implementation_plan.md.
"""


def main() -> None:
    print(
        "proctor 0.1.0 — orchestration framework scaffolding (M0).\n"
        "No verbs are implemented yet; see "
        "plan_docs/orchestration_framework_implementation_plan.md."
    )
